# PASO Microsoft Teams

Official PASO channel plugin for Microsoft Teams bot conversations.

Install from PASO:

```bash
openclaw plugins install @openclaw/msteams
```

Configure the Teams bot credentials and trusted service URLs in PASO, then connect the bot to the teams or chats where agents should operate.
