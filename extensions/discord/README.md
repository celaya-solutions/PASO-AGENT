# PASO Discord

Official PASO channel plugin for Discord servers, channels, DMs, slash commands, and app events.

Install from PASO:

```bash
openclaw plugins install @openclaw/discord
```

Configure a Discord bot token and the channels or servers PASO should handle. The plugin lets PASO agents receive Discord messages and respond through the configured Discord app.
