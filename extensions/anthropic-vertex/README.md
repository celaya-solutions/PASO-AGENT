# PASO Anthropic Vertex Provider

Official PASO provider plugin for Claude models hosted through Google Vertex AI.

Install from PASO:

```bash
openclaw plugins install @openclaw/anthropic-vertex-provider
```

Configure Google Cloud credentials and the target Vertex project/region in PASO, then select Claude models with the Anthropic Vertex provider.
