# PASO Matrix

Official PASO channel plugin for Matrix rooms and direct messages.

Install from PASO:

```bash
openclaw plugins install @openclaw/matrix
```

Configure the Matrix homeserver and bot credentials in PASO. The plugin lets agents join configured rooms, receive messages, and reply through Matrix.
